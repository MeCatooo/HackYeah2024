import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class LiteSurveyScreen extends ConsumerWidget {
  const LiteSurveyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // Implement lite survey form
    return Scaffold(
        appBar: AppBar(title: const Text('Ankieta tygodniowa')),
        body: // Form widgets
    );
  }
}