import 'package:flutter/material.dart';

class TreesTab extends StatelessWidget {
  const TreesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Zieleń (Trees) Tab Content'));
  }
}
